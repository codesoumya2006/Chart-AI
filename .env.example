# Gemini API Key - Get from Google Cloud Console
# Required for AI features (Explain, Quiz, Flow Generation, etc.)
# Frontend key must be restricted to JavaScript browser API via Google Cloud Console
VITE_GEMINI_API_KEY=your_gemini_api_key_here

# Firebase Configuration - Exact values (do not change these)

